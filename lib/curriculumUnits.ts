import type { GradeBand } from "./blueprint";

/**
 * 교과서 단원 목록 — 문항 상세의 「교과 단원」 고르개가 쓴다.
 *
 * 단원을 손으로 적던 칸이었다. 그러면 같은 단원이 「낱말의 의미 관계」·「낱말 관계」·
 * 「2. 낱말의 뜻」으로 흩어져 명세표 배정과 맞춰 볼 수가 없고, 학년을 옮기고 단원은
 * 그대로 두는 일도 막지 못한다. 학년에 맞는 교과서 단원 중에서 고르게 한다.
 *
 * 과목을 따로 고르지 않는다. 「국어 - 생생하게 표현해요」를 고르면 과목이 함께 정해진다 —
 * 과목과 단원을 따로 두면 「수학 · 생생하게 표현해요」가 생길 수 있다.
 *
 * ── 무엇을 옮겼나 ──
 * 2022 개정 교육과정 교과서. 3·4학년은 2025년, 5·6학년은 2026년부터 모두 이 교과서를 쓴다.
 * BASIS에 과목마다 무엇을 기준으로 옮겼는지 적어 두었다 — 국어는 국정이라 한 벌이지만
 * 수학·과학은 검정이라 출판사마다 단원 이름과 차례가 조금씩 다르다. 2026-09-14에 교사 지원
 * 사이트 · EBS 강좌 목록 · 교육청과 학교의 수업-평가 계획으로 두 번씩 따로 대조했다.
 *
 * ⚠ 교과서가 바뀌면 이 파일을 고친다. 이미 고른 단원은 문항에 이름·번호·학년-학기로
 *   남아 있으므로(ItemDraft.unit · unitNo · unitTerm), 목록에서 빠져도 문항은 그대로다.
 *   고르개가 그 문항을 「목록 밖」으로 세운다.
 */

export type UnitSubject = "국어" | "수학" | "과학";

/** 국어의 번호 없는 단원 — 국어 가 첫머리의 독서 단원, 국어 나 첫머리의 매체 단원 */
export type UnitKind = "독서" | "매체";

export type TextbookUnit = {
  subject: UnitSubject;
  grade: 3 | 4 | 5 | 6;
  term: 1 | 2;
  /** 그 학기 교과서의 몇 단원인가. 번호 없는 단원(독서 · 매체)은 null */
  no: number | null;
  /** 번호 없는 단원의 갈래 */
  kind?: UnitKind;
  title: string;
};

const SUBJECTS: UnitSubject[] = ["국어", "수학", "과학"];

/** 과목마다 무엇을 기준으로 옮겼나 */
export const BASIS: Record<UnitSubject, string> = {
  국어:
    "국정 교과서의 차례(국어 가·나를 학기마다 합침). 학기마다 번호 없는 단원이 둘 있다 — 가 " +
    "첫머리의 독서 단원, 나 첫머리의 매체 단원. 교과서 차례 그대로 그 자리에 둔다.",
  수학:
    "검정 교과서 9종의 차례에서 가장 많은 출판사가 쓰는 이름과 차례. 학기에 따라 한두 표 " +
    "차로 갈린 이름이 있다 — 5-2 「수의 범위와 어림하기」(5:4 「수의 범위와 올림, 버림, " +
    "반올림」)·「직육면체」(5:4 「직육면체와 정육면체」), 6-1 「여러 가지 그래프」(5:3 " +
    "「띠그래프와 원그래프」), 6-2 「원의 둘레와 넓이」(5:3 「원의 넓이」) 등. 출판사에 따라 " +
    "단원이 다른 학기에 들어간 경우도 있다(4-1 · 4-2).",
  과학:
    "2022 개정 과학과 교육과정의 주제 순서(3·4학년 [4과01]~[4과16], 5·6학년 " +
    "[6과01]~[6과16])를 한 학기에 넷씩. 검정 교과서 대단원 이름과 글자까지 같다. 5-2만 " +
    "비상·미래엔이 2·3단원을 바꿔 싣는다(열과 우리 생활 → 날씨와 우리 생활).",
};

type Entry = string | [UnitKind, string];
type TermKey = `${3 | 4 | 5 | 6}-${1 | 2}`;

/** 학년-학기 → 교과서 차례대로. 글자는 번호 단원, [갈래, 이름]은 번호 없는 단원 */
const RAW: Record<UnitSubject, Record<TermKey, Entry[]>> = {
  국어: {
    "3-1": [
      ["독서", "읽은 책을 소개해요"],
      "생생하게 표현해요",
      "분명하고 유창하게",
      "짜임새 있는 글, 재미와 감동이 있는 글",
      ["매체", "서로 배려하며 소통해요"],
      "중요한 내용을 찾아요",
      "인물에게 마음을 전해요",
      "자신 있게 읽고 써요",
    ],
    "3-2": [
      ["독서", "계획대로 차근차근 읽어요"],
      "경험과 관련지으며 이해해요",
      "유창하게 읽고 발표해요",
      "정확하게 글을 써요",
      ["매체", "온라인 상황에서 글을 써요"],
      "서로 존중하며 대화해요",
      "사전으로 여는 세상",
      "감상과 표현의 즐거움",
    ],
    "4-1": [
      ["독서", "질문을 만들며 책을 읽어요"],
      "깊이 있게 읽어요",
      "서로 다른 의견",
      "자세하게 살펴요",
      ["매체", "인터넷에서 자료를 찾아요"],
      "뜻을 파악하며 읽어요",
      "말과 글로 전하는 생각",
      "경험을 표현해요",
    ],
    "4-2": [
      ["독서", "생각을 나누며 책을 읽어요"],
      "비교하며 읽어요",
      "우리말 우리글",
      "의견을 모아서",
      ["매체", "자료를 활용하여 발표해요"],
      "책 속의 길을 따라",
      "오가는 마음",
      "상상의 날개",
    ],
    "5-1": [
      ["독서", "책을 읽고 함께 이야기해요"],
      "대화를 나누어요",
      "체험한 일을 글로 써요",
      "발표하고 질문해요",
      ["매체", "필요한 정보를 찾아요"],
      "대상을 설명해요",
      "의논하며 토의해요",
      "작품을 즐겨요",
    ],
    "5-2": [
      ["독서", "여러 가지 형태의 책을 읽어요"],
      "작품에 대한 의견을 나누어요",
      "올바른 문장으로 글을 써요",
      "표준어와 방언을 알아봐요",
      ["매체", "판단하며 이용해요"],
      "의견을 조정해요",
      "추론하며 읽어요",
      "작품을 감상해요",
    ],
    "6-1": [
      ["독서", "같은 주제에 대한 책을 읽고 생각을 나누어요"],
      "자신의 삶과 관련지어 읽어요",
      "바르게 고쳐 써요",
      "절차를 지키며 토론해요",
      ["매체", "매체 자료를 만들어요"],
      "상황에 맞게 표현해요",
      "자신의 글쓰기 과정을 살펴봐요",
      "비판적으로 읽어요",
    ],
    "6-2": [
      ["독서", "다양한 책을 읽고 문제를 해결해요"],
      "깊이 있게 감상해요",
      "궁금한 점을 해결해요",
      "보거나 듣고 판단해요",
      ["매체", "매체 이용 습관을 돌아봐요"],
      "소중한 우리말",
      "언어와 소통",
      "시와 이야기로 표현해요",
    ],
  },
  수학: {
    "3-1": ["덧셈과 뺄셈", "평면도형", "나눗셈", "곱셈", "길이와 시간", "분수와 소수"],
    "3-2": ["곱셈", "나눗셈", "원", "분수", "들이와 무게", "그림그래프"],
    "4-1": ["큰 수", "각도", "곱셈과 나눗셈", "평면도형의 이동", "막대그래프", "규칙 찾기"],
    "4-2": [
      "분수의 덧셈과 뺄셈",
      "삼각형",
      "소수의 덧셈과 뺄셈",
      "사각형",
      "꺾은선그래프",
      "다각형",
    ],
    "5-1": [
      "자연수의 혼합 계산",
      "약수와 배수",
      "대응 관계",
      "약분과 통분",
      "분수의 덧셈과 뺄셈",
      "다각형의 둘레와 넓이",
    ],
    "5-2": [
      "수의 범위와 어림하기",
      "분수의 곱셈",
      "합동과 대칭",
      "소수의 곱셈",
      "직육면체",
      "평균과 가능성",
    ],
    "6-1": [
      "분수의 나눗셈",
      "각기둥과 각뿔",
      "소수의 나눗셈",
      "비와 비율",
      "여러 가지 그래프",
      "직육면체의 겉넓이와 부피",
    ],
    "6-2": [
      "분수의 나눗셈",
      "공간과 입체",
      "소수의 나눗셈",
      "비례식과 비례배분",
      "원의 둘레와 넓이",
      "원기둥, 원뿔, 구",
    ],
  },
  과학: {
    "3-1": ["힘과 우리 생활", "동물의 생활", "식물의 생활", "생물의 한살이"],
    "3-2": ["물체와 물질", "지구와 바다", "소리의 성질", "감염병과 건강한 생활"],
    "4-1": ["자석의 이용", "물의 상태 변화", "땅의 변화", "다양한 생물과 우리 생활"],
    "4-2": ["밤하늘 관찰", "생물과 환경", "여러 가지 기체", "기후변화와 우리 생활"],
    "5-1": ["지층과 화석", "빛의 성질", "용해와 용액", "우리 몸의 구조와 기능"],
    "5-2": ["혼합물의 분리", "날씨와 우리 생활", "열과 우리 생활", "자원과 에너지"],
    "6-1": ["산과 염기", "물체의 운동", "식물의 구조와 기능", "지구의 운동"],
    "6-2": ["계절의 변화", "물질의 연소", "전기의 이용", "과학과 나의 진로"],
  },
};

export const textbookUnits: TextbookUnit[] = SUBJECTS.flatMap((subject) =>
  (Object.entries(RAW[subject]) as [TermKey, Entry[]][]).flatMap(([term, entries]) => {
    const [g, t] = term.split("-").map(Number);
    let no = 0;
    return entries.map((e): TextbookUnit => {
      const base = { subject, grade: g as TextbookUnit["grade"], term: t as TextbookUnit["term"] };
      if (typeof e !== "string") return { ...base, no: null, kind: e[0], title: e[1] };
      no += 1;
      return { ...base, no, title: e };
    });
  }),
);

/** 「3-1」 */
export const termOf = (u: Pick<TextbookUnit, "grade" | "term">) => `${u.grade}-${u.term}`;

/** 고르개의 값 — 같은 이름이 학기마다 있어서(수학 「분수의 나눗셈」) 이름만으로는 못 가린다 */
export const unitKey = (u: TextbookUnit) => `${u.subject}|${termOf(u)}|${u.title}`;

/** 「국어 - 생생하게 표현해요」 */
export const unitLabel = (u: Pick<TextbookUnit, "subject" | "title">) =>
  `${u.subject} - ${u.title}`;

/** 「1단원」 · 「독서 단원」 */
export const unitPlace = (u: TextbookUnit) => (u.no === null ? `${u.kind} 단원` : `${u.no}단원`);

/** 문항에 적는 단원 번호 — 두 자리. 번호 없는 단원은 비운다 */
export const unitNoOf = (u: TextbookUnit) => (u.no === null ? "" : String(u.no).padStart(2, "0"));

const bandGrades = (band: GradeBand) => (band === "3-4" ? [3, 4] : [5, 6]);

/** 학년에 드는 단원 — 과목 → 학년 → 학기 → 교과서 차례 */
export function unitsForBand(band: GradeBand): TextbookUnit[] {
  const grades = bandGrades(band);
  return textbookUnits.filter((u) => grades.includes(u.grade));
}

/** 고르개의 묶음 — 「국어 · 3학년 1학기」마다 한 덩이 */
export function unitGroups(band: GradeBand) {
  const groups: { label: string; units: TextbookUnit[] }[] = [];
  for (const u of unitsForBand(band)) {
    const label = `${u.subject} · ${u.grade}학년 ${u.term}학기`;
    const last = groups[groups.length - 1];
    if (last?.label === label) last.units.push(u);
    else groups.push({ label, units: [u] });
  }
  return groups;
}

/**
 * 문항에 적힌 단원이 목록의 어느 단원인가. 목록 밖이면 null.
 *
 * 목록에서 고른 문항은 과목 · 학년-학기 · 이름이 모두 같아야 찾은 것으로 본다. 번호로 찾지
 * 않는다 — 교과서가 바뀌어 같은 번호에 다른 단원이 들어서면, 번호로 찾아 새 이름을 붙여 주는
 * 순간 출제위원이 고른 적 없는 단원이 문항에 붙는다. 이름이 달라졌으면 목록 밖으로 세워 다시
 * 고르게 한다.
 *
 * 고르개가 생기기 전의 문항(학년-학기 없음)은 과목과 이름으로만 찾는다.
 */
export function findUnit(
  item: { subject: string; unit: string; unitNo: string; unitTerm: string },
  band: GradeBand,
): TextbookUnit | null {
  const title = item.unit.trim();
  if (!title) return null;
  const same = unitsForBand(band).filter((u) => u.subject === item.subject && u.title === title);
  if (item.unitTerm) return same.find((u) => termOf(u) === item.unitTerm) ?? null;
  /* 학년-학기가 없는 옛 문항. 같은 이름이 여러 학기에 있으면(수학 「분수의 나눗셈」) 짐작하지
     않는다 — 짐작한 학년·학기를 옆에 적어 두면 고른 것으로 읽혀 아무도 다시 고르지 않는다.
     적어 둔 단원 번호로 하나가 가려질 때만 그것으로 본다 */
  if (same.length === 1) return same[0];
  const byNo = same.filter((u) => u.no !== null && u.no === Number(item.unitNo));
  return byNo.length === 1 ? byNo[0] : null;
}
