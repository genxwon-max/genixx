import AiJobsView from "./AiJobsView";

export const metadata = { title: "AI 프롬프트" };

/*
 * ADM-13-1 AI 프롬프트 — AI에게 무엇을 시키는지 한자리에 편다.
 *
 * 시스템 설정(ADM-13)의 아래 자리로 둔다. 저 화면은 「지금 무엇이 어떤 값으로 걸려 있나」를
 * 읽기만 하는 자리이고, 이 화면은 그 가운데 **운영자가 실제로 손댈 수 있는 한 갈래**다.
 * 임계값(0.75 · 0.80 · ±0.25)은 저쪽에 그대로 두고 여기서는 고칠 수 없게 잠근다 — 그 숫자는
 * 공개 존과 FAQ에 대외 약속으로 나가 있다.
 *
 * ── 왜 이 화면이 필요한가 ──
 * 여태 AI에게 무엇을 시키는지가 저장소 파일의 주석과 함수 사이에 흩어져 있었다. 산출물이
 * 이상할 때 「무엇을 시켰길래 이렇게 나왔나」를 물을 자리가 콘솔에 없었고, 한 줄을 고치려면
 * 배포를 해야 했다. 리포트 문구를 코드에서 떼어 해석 템플릿(ADM-08-1)으로 옮긴 것과 같은
 * 일을 프롬프트에 한다.
 *
 * ── 목록이 답하는 첫 물음 ──
 * 「AI가 이 제품 어디에 서 있나」다. 그래서 표의 가장 넓은 칸이 단계 줄이다 — 문항 생성
 * 하나에도 AI가 네 번 돌고 마지막에 규칙이 한 번 더 돈다는 것이 목록에서 바로 보여야,
 * 상세에 들어가서 「1차 AI · 2차 AI」를 마주쳤을 때 놀라지 않는다.
 *
 * ── 고치는 것 셋 ──
 * 프롬프트의 여는 칸 · 부르는 모델 · 호출 값(창의성·최대 길이)이다. 셋 다 단계마다 따로 걸고,
 * 저장 단추 하나가 함께 저장한다 — 저장이 둘이면 모델만 바꾸고 나가는 사람이 생기고, 그때
 * 프롬프트 초안이 소리 없이 사라진다.
 *
 * ⚠ 연결 키와 엔드포인트는 여기서 고치지 않는다. 저것은 고르는 값이 아니라 비밀이고, 화면에
 *   한 번 내려오면 「가렸다」가 개발자 도구 앞에서 거짓이 된다. 자리는 시스템 설정(ADM-13)이다.
 *
 * ⚠ 리포트 문구는 이 목록에 없다. 리포트는 LLM이 짓지 않고 검수된 문구를 규칙으로
 *   조립한다(lib/reportAssets.ts). 고치는 자리는 해석 템플릿이다. 「AI 관리」라는 이름을
 *   보고 리포트 문장을 고치러 온 사람이 여기서 헤매지 않도록 화면 아래에 그렇게 적어 둔다.
 *
 * ⚠ 규칙이 도는 단계도 목록에서 빼지 않는다. 검사지 조합 제안은 이름에 AI가 붙어 있지만
 *   실제로 도는 것은 규칙 하나다 — 그 사실을 적어 두지 않으면 조합을 고치러 온 사람이
 *   여기서 고칠 것을 찾다가 없어서 돌아간다.
 */
export default function Admin2AiPage() {
  return <AiJobsView />;
}
