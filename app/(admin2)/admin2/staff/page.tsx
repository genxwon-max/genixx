import StaffView from "./StaffView";

export const metadata = { title: "운영자·권한" };

/*
 * ADM-03 운영자·권한.
 *
 * 이 콘솔에서 슈퍼 관리자만 여는 화면이고, 여기서 답하는 질문은 하나다 —
 * **누가 들어올 수 있는가.** 계정 · 역할 · 2단계 인증 · 상태를 목록 한 장으로 편다.
 *
 * ── 대조표를 걷어 냈다 ──
 * 목록 아래에 「역할 × 권한」 대조표를 세로로 쌓아 두었다. 역할 넷의 기본값을 ●/−로
 * 그리는 열여덟 줄짜리 표였는데, 상세(ADM-03-1)가 생기면서 하는 일이 겹쳤다. 「이 사람이
 * 무엇을 할 수 있나」는 이제 그 사람의 상세가 켜진 칸으로 답하고, 그쪽은 사람마다 더하고
 * 뺀 것까지 함께 그린다. 대조표는 역할 기본값만 그려서, 둘이 어긋나 보이는 날이면 어느
 * 쪽이 지금 값인지 화면이 스스로 답하지 못했다.
 *
 * 지표 띠(Kpi)를 두지 않았다. 넉 줄짜리 띠를 얹으면 표 머리가 100px 밀린다. 대신 그 숫자를
 * 조회 조건 탭으로 바꿨다 — 활성·정지/휴면·2단계 미설정을 누르면 아래 목록이 그 묶음만
 * 남는다. 2단계 미설정을 탭으로 올린 것은, 이 화면에서 유일하게 「오늘 해야 하는 일」인
 * 숫자라서다. 머리·탭·목록은 StaffView(클라이언트)가 맡는다.
 *
 * ⚠ 계정·이름은 전부 예시다(lib/adminUsers.ts). 역할과 권한의 정의만은 예시가 아니라
 *   실제 값으로, lib/admin.ts의 staffRoles.permissions를 그대로 읽는다.
 *
 * ⚠ 출제자에게 item.review가, 검수자에게 item.write가 없는 것은 빠뜨린 것이 아니라
 *   이해충돌을 막으려고 맞물려 갈라 둔 칸이다(정의서 9장). 대조표 아래 적어 두었던 말이라
 *   화면에서는 사라졌지만, 규칙 자체는 lib/admin.ts의 staffRoles 주석이 들고 있다.
 */

export default function Admin2StaffPage() {
  return <StaffView />;
}
