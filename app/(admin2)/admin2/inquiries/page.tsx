import InquiriesView from "./InquiriesView";

export const metadata = { title: "문의" };

/*
 * ADM-10 문의 — 들어온 문의를 한 장에 모아 둔다.
 *
 * 이 화면을 여는 사람이 던지는 질문은 둘이다.
 *   ① 오늘 답해야 할 것이 얼마나 쌓였고, 그중 약속을 깬 것이 몇인가   탭 한 줄
 *   ② 그중 무엇부터 여나                                          표 한 장
 *
 * 숫자 다섯을 읽기만 하는 지표 띠로 두었을 때는, 「목표 초과 4건」을 보고 나서 그 넷을
 * 찾으러 아래 거르개로 내려가야 했다. 지금은 그 숫자가 곧 조회 조건이다 — 누르면 표가
 * 그 묶음만 남는다. 그래서 표의 상태·목표 거르개는 걷어 냈다. 같은 조건을 두 군데서
 * 걸면 서로 부딪친다.
 *
 * 머리·탭·표는 InquiriesView(클라이언트)가 맡는다. 탭이 조회 조건이라 고른 것을 들고
 * 있어야 하기 때문이다. 여기에는 문서 제목만 남는다.
 *
 * ⚠ 응답 목표 24시간은 데이터(overdue)가 이미 판정해 온 값이다. 화면에서 다시 계산하지
 *   않는다 — 화면과 데이터가 각자 기준을 들면 둘이 어긋나는 날이 온다.
 *
 * ⚠ 오른쪽 동작 단추를 두지 않았다. 이 화면에서 할 수 있는 일은 줄 단위 답변뿐이고,
 *   그 단추는 표 오른쪽 끝에 있다. 머리에 동작하지 않는 「내보내기」를 세우지 않는다.
 *
 * 개인정보: 작성자는 원본이 이미 가려 둔 형태(김****)로 온다. 연락처·메일·생년월일은
 * 데이터에도 없고 칸도 만들지 않는다.
 *
 * ⚠ 숫자는 전부 예시다(lib/admin.ts).
 */
export default function Admin2InquiriesPage() {
  return <InquiriesView />;
}
