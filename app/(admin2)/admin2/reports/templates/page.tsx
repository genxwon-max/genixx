import TemplatesView from "./TemplatesView";

export const metadata = { title: "해석 템플릿" };

/*
 * ADM-08-1 해석 템플릿 — 리포트에 조립되는 문구.
 *
 * 이 서비스가 파는 것은 「사람이 확정한 판정」이고, 그 판정을 이루는 문장은 LLM이 그때그때
 * 지어내지 않는다. 검수된 문구를 규칙으로 조립한다. 그런데 여태 그 문구가 코드에 박혀
 * 있어서(lib/result.ts의 expertNotes·directionMap) 한 줄을 고치려면 배포를 해야 했고,
 * 정작 고쳐야 하는 사람은 손댈 자리가 없었다. 이 화면이 그 자리다.
 *
 * ── 학년대로 가른다 ──
 * 초3에게 하는 말과 중2에게 하는 말이 같을 수 없다. 그런데 옛 문구는 학년 구분이 없어
 * 여덟 살 터울이 같은 글을 받고 있었다. 탭이 곧 학년대다.
 *
 * ⚠ 학년 축은 SurveyBand(초3~4·초5~6·중1·중2~3)를 쓴다. 문항 은행의 GradeBand(3-4·5-6)는
 *   성취기준 코드 접두에 매인 값이라 **중학교가 없고**, 리포트는 중학생에게도 나간다.
 *   lib/surveyBands.ts가 두 갈래를 일부러 갈라 둔 까닭이 그것이다.
 *
 * ── 빈 칸을 숨기지 않는다 ──
 * 아직 아무도 쓰지 않은 칸을 목록에서 빼면 「중1 문구를 다 썼다」고 착각한 채로 리포트가
 * 나간다. 빈 칸도 줄로 세우고 상태로 표시한다 — 이 화면이 답해야 하는 첫 물음이
 * 「어느 칸이 비었나」다.
 *
 * ⚠ 고친 문구는 **다음 조립부터** 나간다. 이미 발행된 리포트는 조립될 때 문장을 제 블록에
 *   복사해 담았으므로 여기서 무엇을 고쳐도 그대로다 — 보호자가 이미 읽은 글이 뒤에서
 *   소리 없이 바뀌면 안 된다.
 *
 * ⚠ 라벨링 금칙어가 든 문구는 저장이 막힌다. 리포트 승인 화면은 아이 하나의 문구를 막지만
 *   템플릿은 그 문구를 받는 **모든** 아이에게 나간다.
 */
export default function Admin2ReportTemplatesPage() {
  return <TemplatesView />;
}
