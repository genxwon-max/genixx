import RulesView from "./RulesView";

export const metadata = { title: "조립 규칙" };

/*
 * ADM-08-4 조립 규칙 — 어떤 조건에 어떤 블록이 붙는가.
 *
 * 리포트 검토 화면(EXP-08)의 블록마다 「R-04 · 최상위 축에 붙는 해석 블록」 같은 근거 줄이
 * 서 있는데, 그 R-번호가 가리키는 규칙이 저장소 어디에도 없었다. ensureReport()가 조립할
 * 때 문자열로 적어 넣은 것이 전부였다. 검토자는 근거를 읽고도 그 규칙을 확인할 자리가
 * 없었고, 규칙을 고치려면 배포를 해야 했다.
 *
 * ── 무엇을 고칠 수 있나 ──
 * 켜고 끄기 · 차례 · 근거 줄 · 조건의 숫자. 조건 자체는 다섯 갈래로 못 박고 그 안의 값만
 * 고치게 한다. 「score > 70 && surveys < 2」 같은 자유 식을 적게 하면 운영자가 리포트를
 * 통째로 멈추는 식을 쓸 수 있고, 그것이 틀렸는지는 아이 리포트가 나간 뒤에야 안다.
 *
 * ── 미리보기를 옆에 둔 까닭 ──
 * 규칙을 고치는 사람이 알고 싶은 것은 「이걸 바꾸면 리포트가 어떻게 달라지나」 하나다.
 * 표본 값을 세워 놓고 어떤 블록이 붙고 떨어지는지를 그 자리에서 보인다.
 *
 * ⚠ 진짜 아이 자료로 미리 보이지 않는다. 발행된 리포트는 이미 문장을 복사해 담았고, 아직
 *   조립되지 않은 아이의 점수를 끌어오면 이 화면이 개인정보를 펴는 자리가 된다.
 *
 * ⚠ 규칙을 고쳐도 이미 발행된 리포트는 그대로다. 바뀌는 것은 다음에 조립되는 것뿐이다.
 */
export default function Admin2ReportRulesPage() {
  return <RulesView />;
}
