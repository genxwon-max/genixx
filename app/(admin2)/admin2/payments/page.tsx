import PaymentsView from "./PaymentsView";

export const metadata = { title: "결제 내역" };

/*
 * PAY-02 결제 내역 — 얼마가 들어왔나.
 *
 * 이 콘솔의 다른 목록 화면과 성격이 다르다. 저쪽은 「무엇부터 여나」에 답하는 큐이고,
 * 여기는 읽고 나가는 화면이다. 줄마다 누를 것이 없다 — 승인·취소·환불은 결제대행사에서
 * 일어나고 여기로는 결과만 넘어온다(lib/payments.ts 머리 주석).
 *
 * 그래서 이 화면만 숫자를 먼저 세운다. 다른 화면에서 지표 띠를 걷어 낸 것은 그 숫자가
 * 「목록으로 가는 문」이었기 때문인데, 여기서는 숫자 자체가 답이다. 그 답은 하나다 —
 * 「이 기간에 얼마가 남았나」. 순매출만 28px로 세우고 나머지 셋은 22px에 둔다.
 *
 * 조회 기간을 바꾸면 지표와 표가 함께 바뀐다. 지표만 전체 기간이고 표만 이번 달이면
 * 두 수를 견주다가 틀린 결론이 난다 — 그래서 기간 상태를 들고 있는 PaymentsView가
 * 지표까지 함께 맡는다.
 *
 * ⚠ 결제 기록은 전부 예시다(lib/payments.ts). 구매자 이름은 만들 때부터 가려 두었다.
 */
export default function Admin2PaymentsPage() {
  return <PaymentsView />;
}
