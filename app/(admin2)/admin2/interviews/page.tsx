import InterviewsView from "./InterviewsView";

export const metadata = { title: "면담 신청" };

/*
 * EXP-06 면담 신청 — 누가 면담을 기다리고 있고, 그중 무엇부터 잡나.
 *
 * 이 화면을 여는 사람이 던지는 질문은 둘이다.
 *   ① 아직 날짜를 못 잡은 것이 몇이고, 그중 상대가 이미 기다린 것이 있나   탭 한 줄
 *   ② 그중 누구부터 잡나                                                표 한 장
 *
 * 지표 띠를 두지 않았다. 탭이 이미 같은 숫자를 여섯 칸으로 세고 있고, 탭은 누르면 표가
 * 좁아지지만 지표는 안 좁아진다 — 같은 값이 한 화면에 두 번 서면 어느 쪽을 눌러야 하는지
 * 한 박자 멈춘다(문의 화면이 지표 띠를 걷어 낸 것과 같은 까닭).
 *
 * ── 선발과 신청을 한 표에 세운다 ──
 * 자동 선발(pickReasons rank 1~4)과 보호자·교사 신청(rank 5)은 이미 같은 우선순위 표에
 * 함께 서 있는 값이다. 목록을 둘로 가르면 「오늘 잡아야 할 것」을 두 화면에서 두 번 세게
 * 되고, 정작 이 화면이 답해야 하는 「무엇부터」가 두 갈래로 흩어진다.
 *
 * ── 순서를 화면이 정한다 ──
 * 지난 일정 → 신청 접수 → 선발됨(사유 rank) → 일정 잡힘(가까운 날) 순으로 세운다. 사람이
 * 매번 정하게 두면 목소리 큰 신청이 먼저 올라간다 — 자동 정렬의 뜻은 「신청하지 않았지만
 * 꼭 봐야 하는 아이」를 위로 올리는 데 있다(lib/expertStore.ts의 pickReasons).
 *
 * ⚠ 상태 이름은 전문가 콘솔의 것을 그대로 쓴다(lib/expertStore.ts의 interviewStateLabel).
 *   queued를 「일정 미정」이라 불러 보았더니, 탭은 「일정 미정」인데 같은 줄의 상태 칸은
 *   「선발됨」이 되어 한 화면에서 같은 상태가 두 이름으로 섰다.
 *
 * ⚠ 학생 실명을 어느 칸에도 세우지 않는다. 응시번호와 학년으로만 부른다 — 목록은
 *   어깨너머로 가장 잘 보이는 화면이다. 신청자 이름은 가려서(김○○) 세우고 연락처 칸은
 *   아예 만들지 않는다. 전화를 걸 때는 상세를 연다.
 *
 * ⚠ 여기서는 일정을 고치지 않는다. 잡는 자리는 면담 상세 한 장뿐이다 — 목록에서 폼을
 *   펴면 같은 일이 두 화면에서 두 규율로 선다.
 *
 * ⚠ 신청·일정은 브라우저 저장소에만 있다(lib/interviews.ts · lib/interviewStore.ts).
 *   붙일 때는 이 자리에서 면담 API를 부르고 첫 화면을 서버가 그리게 옮긴다.
 *
 * ⚠ 이 화면과 면담 일정(EXP-06-1)은 기둥의 「면담 관리」 그룹에 나란히 선다. 같은 자료를
 *   묻는 것만 달리해 보는 두 그림이라, 어느 한쪽에 다른 쪽으로 가는 단추를 세우지 않는다 —
 *   기둥이 이미 그 길이다.
 */
export default function Admin2InterviewsPage() {
  return <InterviewsView />;
}
