import StudentsView from "./StudentsView";

export const metadata = { title: "학생·접속코드" };

/*
 * ADM-02-1 학생·접속코드.
 *
 * 학생은 스스로 가입하지 않는다. 보호자나 교사가 등록하고, 학생은 8자리 접속코드로만
 * 시험에 들어온다. 그래서 이 화면에서 나오는 질문은 「누가 있나」가 아니라
 * 「코드를 받은 148명 중 아직 안 들어온 사람이 몇인가」다. 탭을 계정 상태가 아니라
 * 응시 상태(examStateLabel)로, 그것도 미응시 → 응시 중 → 제출 완료 → 리포트 발행이라는
 * 진행 순서 그대로 세운 이유가 그것이다. 회차 마감 전날 보는 것은 왼쪽 두 탭이고,
 * 마감 뒤에 보는 것은 오른쪽 두 탭이다.
 *
 * 예전에는 그 다섯을 읽기만 하는 지표 띠로 두었다. 「미응시 41명」을 보고 나서 그
 * 마흔하나를 찾으러 아래 거르개로 다시 내려가야 했으므로, 지금은 숫자가 곧 조회 조건이다.
 *
 * ⚠ 생년월일 칸을 두지 않는다. 이 저장소는 학생 생년월일을 목록에 두지 않기로 했다 —
 *   개인정보 열람은 사유를 받아 감사 로그에 남기는 별도 흐름이고, 목록을 훑는 일에는
 *   필요한 적이 없다. 접속코드는 그 자체가 이 목록의 값이라 그대로 적는다.
 *
 * 머리·탭·표는 StudentsView(클라이언트)가 맡는다. 목록과 탭 개수가 같은 저장소를 봐야
 * 하기 때문이다 — 서버에서 세어 박아 두면 상세에서 무언가 고치고 돌아온 화면에서
 * 탭만 옛 수로 남는다.
 */
export default function Admin2Students() {
  return <StudentsView />;
}
