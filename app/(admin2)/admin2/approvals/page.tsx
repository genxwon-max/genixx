import ApprovalsView from "./ApprovalsView";

export const metadata = { title: "가입 승인" };

/*
 * ADM-02-2 가입 승인 — 슈퍼 관리자용.
 *
 * ── 판 목록에서 표로 되돌린 까닭 ──
 * 한동안 줄마다 판(Panel)을 세웠다. 다섯 줄뿐이고 한 건에서 읽을 것이 길다는 이유였는데,
 * 그러면 이 화면이 할 수 있는 일이 「위에서 아래로 다섯 장 읽기」뿐이 된다. 승인·반려를
 * 실제로 처리하게 만들고 나니 필요한 것이 늘었다 — 처리한 건과 안 한 건을 가르는 탭,
 * 어느 건인지 되찾는 검색, 종류·경고로 좁히는 거르개. 판 목록에는 그것들을 붙일 자리가
 * 없다. 무엇보다 콘솔의 다른 열두 화면과 손이 가는 자리가 달랐다.
 *
 * 한 건에서 길게 읽어야 하는 것(신청 내용 · 제출 증빙 · 자동 점검 사유)은 상세 화면
 * (ADM-02-2-1)으로 옮겼다. 목록은 **어느 건을 열지** 고르는 데 필요한 것만 세운다.
 *
 * ── 일부러 두지 않은 것 ──
 * 일괄 승인 체크상자: 증빙을 읽지 않고 여러 건을 한 번에 넘기게 만드는 장치다. 승인은
 * 한 건씩 상세를 열어 읽고 누른다.
 * 신청자 연락처·생년월일: 승인 판단에 필요 없다.
 *
 * ⚠ 이름·기관·시각은 전부 예시다(lib/admin.ts). 처리 결과는 이 브라우저에만 남는다
 *   (lib/approvalStore.ts) — 서버가 없으므로 실제 계정이 열리지는 않는다.
 */
export default function Admin2Approvals() {
  return <ApprovalsView />;
}
